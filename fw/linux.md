# Linux Hacking

- /proc/cmdline: parameters passed to the kernel by GRUB or other bootloaders
- /proc/iomem: physical memory map
- /boot/config-$(uname -r): config parameters the kernel was compiled with
