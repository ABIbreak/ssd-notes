# Week 6: February 23rd - March 1st

## Saturday
- NVMeVirt's model of OOB data is insufficient:
  * the FTL directly manages the reverse lookup table from physical pages to logical pages
  * only tracks P/E cycles, and the GC does not take it into account
  * does not timestamp writes, which is important for recovering the mapping table
- May need to modify it such that it runs:
    - a recovery procedure, restoring contents from a file
	- a crash procedure, such as upon module removal, where all writes before the flush are persisted to a file, and all writes after are randomly discarded
- Started using Claude Code to rewrite `conv_ftl.c`, which burned up 40 minutes of my time and a lot of tokens, so resorted to having it fill out a TODO list and checking along the way

## Sunday
* Got Claude to translate `conv_ftl.c` to a Rust version, but having trouble building the module
    * Copied `/usr/lib/modules/$(uname -r)/build` locally in order to remove `-Zno-jump-tables` from `arch/x86/Makefile`
    * Tinkered around with the Makefiles to generate `scripts/target.json` for rustc
    * Need to figure out how to get the system to generate the core crate
* Looked into FEMU as an alternative
    * Much of the FTL code looks similar to that of NVMeVirt; 
